/**
 * utils.h
 * Module to provide helper functions to threads
*/

#ifndef _UTILS_H_
#define _UTILS_H_

// Taken from the assignment 1 instructions
// Causes the thread to sleep for a specified amount of ms
void Utils_sleepForMs(long long delayInMs);

#endif